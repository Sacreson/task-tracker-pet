// Отключаем создание исполняемого jar (это просто библиотека)
bootJar {
    enabled = false
}

jar {
    enabled = true
}

dependencies {
    // Сюда будем класть зависимости, общие для всех (например, DTO валидации)
    // Пока пусто, lombok и test уже прилетели из корня
}